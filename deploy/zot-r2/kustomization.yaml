apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  # Copy secret.example.yaml to secret.yaml after filling credentials.
  # - secret.yaml
  # Copy ingress.example.yaml to ingress.yaml after setting hostname/TLS.
  # - ingress.yaml
