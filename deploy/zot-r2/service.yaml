apiVersion: v1
kind: Service
metadata:
  name: omegon-armory-zot
  namespace: omegon-armory
  labels:
    app.kubernetes.io/name: omegon-armory-zot
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: omegon-armory-zot
  ports:
    - name: http
      port: 5000
      targetPort: http
