# TypeScript Shop

A TypeScript-oriented engineering profile. Use for strict TS, Node, ESM tooling, web apps, and automation codebases.

This profile is a curated stack: install it as a starting point, then adjust optional dependencies for the project.
