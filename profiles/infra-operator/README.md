# Infra Operator

An infrastructure-operator profile for remote changes, OCI packaging, runbooks, and production-oriented validation.

This profile is a curated stack: install it as a starting point, then adjust optional dependencies for the project.
